using System;

namespace AcmeAccounting
{
    class Program
    {
        static void Main()
        {
            // the \n makes the string after appear on the next line. the \" is use to create " withing the string that the compiler dosent use.
            Console.WriteLine(" Welcom to Acme Accounting Systems \n Remember we're \"Accounting\" on you!"); // This will ask the user there name. they will enter in below in the string = name.
            Console.Read(); // without this line the program will run and then be set so the next key press closes the cmd window.
        }
    }
}

